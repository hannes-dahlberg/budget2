import Account from './models/account';

/*Account.all().then((accounts: Account[]) => {
    accounts.forEach((account: Account) => account.subAccounts().then((accounts: Account[]) => {
        console.log('SUB ACCOUNTS', accounts);
    }));
    console.log('ACCOUNTS', accounts);
});*/